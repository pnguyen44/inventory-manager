<nav class="navbar navbar-inverse bg-primary">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          {{!-- <li>
            {{#link-to "users"}}Users{{/link-to}}
          </li> --}}
          <li>
          {{#link-to "categories"}}Inventory{{/link-to}}
          </li>
          <li>
          {{#link-to "running-low"}}Running Low{{/link-to}}
          </li>
          <li>
          {{#link-to "order-list"}}Order List{{/link-to}}
          </li>

        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
          <li>
            {{#link-to "change-password"}}Change Password{{/link-to}}
          </li>
          <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
          <li>
            {{#link-to "sign-up"}}Sign Up{{/link-to}}
          </li>
          <li>
            {{#link-to "sign-in"}}Sign In{{/link-to}}
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-10 col-md-offset-1">
  {{outlet}}
</div>
